# SOP组装行为分析看板（静态原型）

## 快速运行
```bash
./run.sh
```
默认端口 `8080`，打开浏览器访问：

- http://127.0.0.1:8080/index.html

## 可选端口
```bash
./run.sh 9000
```

## 功能说明
- 模拟工业看板界面布局（顶部状态、主监控区、右侧KPI与统计、底部控制）。
- 前端使用 `app.js` 动态渲染步骤执行数据和统计数据。
- 提供按钮交互：开始 / 停止 / 待机 / 清零。
- 模拟步骤流转与计时变化，便于后续接入后端API。
